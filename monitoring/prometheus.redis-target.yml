# Merge into private Prometheus configuration; exporter has no public port.
scrape_configs:
  - job_name: redis-exporter
    static_configs:
      - targets: [redis-exporter:9121]
        labels: {service: redis, environment: production}
