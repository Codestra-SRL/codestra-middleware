# Lead Automation n8n source

This artifact is inactive and its binding defaults off. Runtime activation requires a separately approved staging binding. n8n may validate and transform allowlisted metadata and return a signed idempotent result only. It must never connect directly to Odoo or PostgreSQL, send communications, place calls, create appointments, or access recording systems.
