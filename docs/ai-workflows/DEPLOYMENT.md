# Staging deployment

Deploy reviewed immutable artifacts only. Apply migration, verify a single head, start workers with bounded leases, and confirm every autonomy kill switch is false. Production activation is outside this mission.
