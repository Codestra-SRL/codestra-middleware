# Workflow definitions

Definitions are immutable versions of steps, dependencies, tools, permissions, approvals, budgets, retries, compensation, and completion criteria. Instances remain pinned to their starting version unless a reviewed migration is recorded.
