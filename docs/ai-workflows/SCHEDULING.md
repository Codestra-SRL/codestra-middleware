# Scheduling

Schedules require explicit IANA timezone, bounded occurrences, start/end, recurrence, calendars, and declared misfire/overlap policy. Server local time is never inferred. Production scheduling remains gated.
