# Events

External events require schema version, tenant, workspace, timestamp, signature, idempotency key, event ID, and correlation ID. Database uniqueness prevents duplicate workflows. Telephony and connector sources remain staging/read-only.
