# Suppression controls

Suppressed, do-not-call, invalid, rejected, and confirmed-duplicate records are blocked before command creation. Live dialing, campaign activation, hopper generation, and arbitrary SQL remain disabled.
