# Controlled canary

Without written approval, run only the 15 synthetic fixtures. An approved real-call canary is capped at three consenting test calls, transcription concurrency one, analysis concurrency one and three known test Odoo updates. Callback activity remains disabled. Restore real-processing flags to the policy-approved state immediately after the canary.

