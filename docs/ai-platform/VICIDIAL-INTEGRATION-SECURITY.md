# VICIdial integration security

Use a least-privilege adapter identity and private transport. The adapter allowlist permits health, read-only discovery, external-key lookup, and approved staging lead creation only. No generic model write, delete, SQL, campaign activation, dial-plan, carrier, or agent operation is exposed.
