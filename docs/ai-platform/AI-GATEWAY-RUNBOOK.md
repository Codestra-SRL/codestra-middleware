# AI Gateway runbook

Check `/api/v1/ai/gateway/health`, gateway request/failure/timeout metrics, and the model registry status. `DISABLED` or `UNAVAILABLE` is expected until the owner supplies private connectivity and credentials. Never print or paste key material; rotate through the approved secret mount.
