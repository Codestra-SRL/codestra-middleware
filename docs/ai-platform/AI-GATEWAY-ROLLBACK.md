# AI Gateway rollback

Set `AI_INFERENCE_ENABLED=false` and `LEAD_DISCOVERY_MOCK_ENABLED=false`, deactivate all CDA-AI workflows, and preserve jobs/audits. Revert model/prompt metadata only through a reviewed migration. Leave Odoo, VICIdial, and Postly write switches false.
