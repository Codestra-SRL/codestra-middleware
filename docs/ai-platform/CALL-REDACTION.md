# Transcript redaction

The default policy masks payment cards, bank accounts, security codes, passwords and tokens before ordinary access or analysis. Normal users receive only protected/redacted text. Any unredacted representation requires explicit policy approval, encryption, separate permission and access audit. Redaction events store category and offsets, not the secret value.

