# Staging canary

The canary is capped at five leads and one concurrent assignment. Target list/campaign must be disabled staging fixtures. Enable `VICIDIAL_LEAD_CREATE_ENABLED` only for the controlled test, then immediately disable it and leave all workflows inactive.
