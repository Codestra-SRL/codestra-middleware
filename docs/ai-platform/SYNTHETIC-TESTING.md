# Synthetic testing

Use only reserved fixture identities and `example.invalid` URLs. Required scenarios include valid lead, duplicate phone, unsupported ownership, malformed model output, timeout, duplicate callback, invalid credential, and blocked Odoo write. No real website request or business lead collection is allowed.
