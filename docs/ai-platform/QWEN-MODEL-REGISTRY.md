# Qwen model registry

Migration `0034_qwen_staging_registry` registers `qwen-primary` and policy `qwen-lead-intelligence-staging`. The model and prompts are `TESTING`, use only `PUBLIC_BUSINESS_DATA` and `INTERNAL_TEST_DATA`, allow two attempts, and do not store credentials. Actual model name, version, serving framework, and private endpoint must be populated only after server inspection.
