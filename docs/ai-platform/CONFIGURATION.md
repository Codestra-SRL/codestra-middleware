# Configuration

AI settings are environment-backed and fail closed. Use secret-file references for provider credentials; never place tokens in database rows, workflow exports, logs, or evidence. Registry endpoint references are identifiers only.
