# Lead Intelligence workflows

Sanitized inactive exports live under `workflows/n8n/ai-platform/`. The chain is mock discovery → mock extraction → Qwen normalization → mock verification → Qwen scoring → signed middleware callback. Fixture data uses `example.invalid`; no live HTTP scraping, browser automation, paid verification, or Odoo import is present.
