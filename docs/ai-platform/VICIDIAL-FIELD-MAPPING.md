# VICIdial field mapping

The allowlisted mapping sends normalized phone, approved contact/address fields, safe summary, and `codestra:<tenant>:vicidial-lead:<lead_record_id>` to the adapter. It never sends hidden reasoning, credentials, or raw source pages.
