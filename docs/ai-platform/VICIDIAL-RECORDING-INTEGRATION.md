# VICIdial recording integration

The approved adapter contract is read-only metadata lookup plus short-lived,
authenticated access. Permanent public URLs and unrestricted filesystem reads
are prohibited. This mission could not inspect 65.21.67.207 because SSH access
was unavailable, so runtime recording status remains unverified.
