# Testing

Use the pinned middleware test image for Python 3.12. Run Ruff, compile checks, targeted AI/lead tests, the migration contract test, and the full suite. Provider, n8n, and Odoo integration tests must use mocks and synthetic `example.invalid` records.
