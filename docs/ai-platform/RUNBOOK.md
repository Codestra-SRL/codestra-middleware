# Runbook

For a stuck job, inspect its attempts, outbox delivery, and result inbox; do not manually change terminal state. For ambiguous outcomes, create or run a dry-run reconciliation. For suspected credential exposure, disable external-provider flags, rotate the secret outside chat, and preserve redacted audit evidence.
