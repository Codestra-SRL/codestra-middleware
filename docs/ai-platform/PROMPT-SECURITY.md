# Prompt security

Prompt versions are immutable after activation and currently `TESTING`. Website and directory text is untrusted data. Prompts explicitly reject embedded instructions, unsupported ownership claims, secret disclosure, external calls, and automatic writes. Hidden chain-of-thought is not requested or stored.
