# One-call safety

The canary gate enforces exactly one lead and one call, rejects duplicate runs,
requires a disabled staging target, and never exposes a generic call operation.
The execution adapter remains a governance contract until infrastructure and
authorization are verified.
