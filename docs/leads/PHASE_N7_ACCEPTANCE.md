# Phase N7 acceptance

Acceptance is demonstrated by deterministic normalization/matching tests, PostgreSQL concurrency and idempotency constraints, reversible merge tests, DNC/consent policy tests, all five attribution models, revenue-event deduplication, workflow validation, migration round-trip, full repository CI, secret/security scans, and bounded synthetic load.

All N7 feature flags default off. Production social posts, Odoo writes, VICIdial commands, and automatic contacts must remain zero. Real Odoo revenue synchronization, real n8n runtime promotion, and external contact execution are explicitly outside this phase.
