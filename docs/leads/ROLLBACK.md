# N7 rollback

First disable `IDENTITY_GRAPH_ENABLED`, `LEAD_INTELLIGENCE_ENABLED`, `NEXT_BEST_ACTION_ENABLED`, `ATTRIBUTION_ENGINE_ENABLED`, and `REVENUE_EVENT_SYNC_ENABLED`. Automatic contacting, social publishing, Odoo writes, and VICIdial commands remain off.

Restore the prior exact middleware image and prior Git-controlled n8n workflows. Canonical rows and audit evidence are preserved. Migration downgrade is for disposable validation only unless governance confirms the N7 tables contain no required records. Re-enable only after health, migration, and policy checks pass.
