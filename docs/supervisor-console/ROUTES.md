# Routes

UI routes live below `/supervisor`. APIs live below `/api/v1/supervisor` and include overview, agents, calls, campaigns, callbacks, QA, compliance, workforce, adherence, schedules and authenticated SSE events. Unknown or out-of-scope resources return 404 to resist IDOR enumeration.
