# Campaigns

Campaign screens are read-only in this release. Targets and thresholds are tenant configuration, not universal constants. Activation, carrier, trunk, dial method and lead mutation are absent from the browser contract and disabled by feature flags.
