# Transfers

Transfer events record requested, accepted, rejected, timeout, abandoned and completed states with ordered sequences. The console calculates success and wait metrics but exposes no arbitrary destination or routing control.
