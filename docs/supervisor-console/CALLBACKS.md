# Callbacks

Callbacks use stable external keys and idempotency hashes. Assignment, reschedule and cancellation require supervisor scope, reason and idempotency. Odoo synchronization is an aggregate-summary operation and remains disabled during this mission.
