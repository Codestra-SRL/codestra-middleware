# Security

Required controls include MFA for privileged roles, field-level authorization, short-lived document access, audit logging, redaction, encrypted storage where supported, rate limits, and tenant/patient/facility isolation.
