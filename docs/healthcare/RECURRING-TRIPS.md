# Recurring trips

Recurring series must generate idempotent future trips, preserve completed history, support exclusions and per-trip overrides, and stop generating trips when authorization or eligibility expires.
