# AI Data Factory

Sources enter an immutable, tenant-scoped ingestion boundary before schema validation,
classification, quality checks, normalization, resolution, and publication. Raw data
is never authoritative by itself.
