# Control Tower architecture

Middleware owns scoped service health, KPI definitions, incidents, actions, approvals, emergency controls, and audit. AI analytics explains observed data but never executes actions.
