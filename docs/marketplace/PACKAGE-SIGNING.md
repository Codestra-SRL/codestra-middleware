# Package signing

Verify immutable digest, publisher signature, trusted signing identity, provenance, SBOM, dependency lock, malware/secret/container scans, compatibility and licensing before staging publication. Mutable tags are never installation references.
