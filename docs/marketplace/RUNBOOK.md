# Runbook

Server A owns plugin lifecycle. Server B owns private AI package execution. Server C owns connector workers/health. Server D owns allowlisted telephony adapter reads. Escalate unsigned packages, capability bypasses, cross-tenant access and sandbox violations.
