# Sandboxing

Executable plugins require isolated non-root workers, resource limits, network allowlists, read-only filesystems, no Docker socket/host filesystem/privileged mode, timeouts and cleanup. Arbitrary host execution is prohibited.
