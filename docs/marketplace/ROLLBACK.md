# Rollback

Rollback addresses runtime, configuration, migrations, workflows, prompts, schemas, UI, entitlements and workers. Destructive database rollback requires separate approval; prefer corrective forward migrations.
