# Manifest

Every immutable version requires plugin identity, publisher, type, version, dependencies, entitlements, capabilities, configuration schema, secret references, health/usage meters, lifecycle steps, digest and trusted signature. Wildcard capabilities and unsigned packages are rejected.
