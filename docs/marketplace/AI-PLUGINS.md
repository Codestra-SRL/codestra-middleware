# AI plugins

AI packages may include reviewed prompts, schemas, model policies, knowledge sources and fixtures. They cannot download arbitrary models, carry credentials, perform hidden calls, bypass tenant filters or write directly to applications.
