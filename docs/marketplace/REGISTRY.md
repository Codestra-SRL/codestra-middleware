# Registry

The private registry stores package, manifest, digest, signature, SBOM, provenance, scan results, release notes and rollback package. Registry access is middleware-mediated and never exposed to browsers or plugins directly.
