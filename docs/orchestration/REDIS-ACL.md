# Redis ACL

Middleware, dispatcher, worker groups, voice, monitoring, and backup identities receive
only required commands/prefixes. FLUSH, CONFIG, MODULE, DEBUG, KEYS, and replication
commands are restricted.
