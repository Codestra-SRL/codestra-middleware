# Enterprise runbook

Phase gates are sequential. On a failed gate, freeze the affected phase, preserve evidence, disable staging workflows, reconcile writes, and require security/governance approval before continuing.
