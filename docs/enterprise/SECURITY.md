# Enterprise security boundaries

Identity assertions require issuer, audience, nonce, tenant binding, and replay checks. Integrations require signed webhooks and idempotency. Analytics require row-level tenant scope. Recovery evidence requires encryption, off-server storage, checksum, and isolated restore validation.
