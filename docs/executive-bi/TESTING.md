# Testing

Test KPI formula contracts, role guards, tenant isolation, stale-source handling, bounded pagination, redaction, export authorization, forecast schema validation and no-action guarantees using synthetic tenants and fixtures. Runtime tests require approved server access and source data.
