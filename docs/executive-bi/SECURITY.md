# BI security

Browser traffic must reach only the middleware BI API. APIs enforce authentication, role checks, tenant/workspace filters, pagination, redaction and audit. Metric labels must not contain customer names, invoice numbers, call IDs or other personal identifiers.
