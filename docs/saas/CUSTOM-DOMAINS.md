# Custom domains

Domains require exact ownership verification, reserved-domain protection, tenant binding, approval, TLS issuance, health checks and safe revocation. Wildcards are disabled by default.
