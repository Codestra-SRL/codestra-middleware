# Entitlements

Entitlements cover portal, users, workspaces, AI requests/tokens, verification, scraper, storage, Agent Assist, BI and telephony. Middleware evaluates them before every restricted operation; clients cannot override limits.
