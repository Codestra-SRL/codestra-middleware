# Security

Required controls include MFA for privileged roles, field-level permissions, short-lived document access, audit logging, redaction, secure uploads, rate limits, replay protection, and tenant/applicant isolation.
