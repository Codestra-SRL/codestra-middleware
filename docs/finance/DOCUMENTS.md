# Documents

Documents use allowlisted types, malware scanning, encrypted tenant-scoped storage, short-lived downloads, retention rules, and access auditing. Raw bank credentials are not stored in normal records.
