# Engagements

Engagement letters are immutable, versioned records. Client and firm signatures are separate states; the system never signs for a client or activates an unsigned engagement. Synthetic e-signature is used only when no provider is configured.
