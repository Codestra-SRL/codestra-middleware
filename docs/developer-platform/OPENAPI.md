# OpenAPI and versions

The public REST API is versioned under `/api/v1`. OpenAPI is the source for reference docs and SDK generation. GraphQL remains a future gateway and is not enabled by this foundation.
