# Deployment

Deploy only to staging after API auth, scope, webhook, sandbox, rate-limit, audit and tenant-isolation tests pass. Public production domains, real billing and production API credentials remain disabled.
