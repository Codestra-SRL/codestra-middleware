# Authentication

The public surface will support OAuth 2.1 with PKCE, scoped API keys and service accounts. Secrets are returned only at creation, stored by reference/hash and rotatable. Internal administrative roles are never inherited by developer applications.
