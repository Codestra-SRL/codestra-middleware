# Rate limiting and analytics

Rate limits are scope-, tenant- and application-aware. Usage analytics are aggregated without customer names, tokens or call IDs as metric labels. Quotas are enforced server-side and return safe retry metadata.
