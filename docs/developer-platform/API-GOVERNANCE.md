# API governance

Every endpoint requires a version, OpenAPI schema, scopes, rate limits, SLA, examples, errors, changelog and deprecation policy. Production activation requires review and tenant-isolation evidence.
