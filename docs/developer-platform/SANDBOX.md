# Sandbox

Sandbox tenants contain synthetic CRM, calls, invoices, projects, mock billing and mock AI/telephony only. Production data and credentials are prohibited. Sandbox API and webhook behavior must be deterministic and resettable.
