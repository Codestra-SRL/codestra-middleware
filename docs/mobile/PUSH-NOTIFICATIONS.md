# Push notifications

APNs/FCM tokens are references, scoped to tenant/device and rotated. Payloads contain safe text and validated deep-link paths only; transcripts, payment data and secrets are excluded.
