# Mobile API contracts

The foundation exposes bootstrap, device registration/revocation, push-token registration, bounded sync and deep-link validation under `/api/v1/mobile`. Business APIs remain middleware-owned and tenant-scoped.
