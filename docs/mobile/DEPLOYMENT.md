# Deployment

Deploy only to internal staging distribution after API auth, tenant isolation, secure storage, sync, accessibility and performance checks pass. Public store publishing remains disabled.
