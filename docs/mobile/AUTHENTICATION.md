# Authentication

Use email/OAuth where approved, MFA, secure refresh rotation, device trust, biometric unlock backed by Keychain/Keystore, session expiry and remote revocation. Tokens never enter logs, URLs, clipboard, AsyncStorage or plain files.
