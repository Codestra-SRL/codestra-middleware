# Runbook

Server A owns mobile API/device/sync state. Server B owns private AI. Server C owns links/assets/health. Server D owns read-only call data. Escalate cross-tenant cache, revoked-device access, unsafe deep links or secret exposure immediately.
