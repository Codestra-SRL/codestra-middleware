# CI/CD

Required checks are lint, type checking, unit/component/API tests, accessibility, dependency/secret scans, binary scans, SBOM and signed internal artifacts. Store Apple/Google signing credentials only in approved secret systems.
