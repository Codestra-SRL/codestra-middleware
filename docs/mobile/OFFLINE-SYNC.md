# Offline sync

Offline records are encrypted, bounded, tenant-scoped and expiring. Mutations use client operation IDs, server idempotency, version checks, conflict handling and visible sync state. Recordings are not cached offline by default.
