# Deep links

Only approved `codestra://` and `https://app.codestra.co` links are accepted. Tokens never appear in links; middleware rechecks authentication, tenant and permission before resolving a resource.
