# Runbook

When an integration is degraded, keep the UI read-only, inspect the health and
reconciliation views, and use the existing service runbook. Never bypass
middleware with browser-side service calls or force a success state.
