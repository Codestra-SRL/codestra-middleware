# Design system

The Control Center reuses the existing React frontend and uses a dense,
dark operational shell with semantic headings, visible state text and responsive
breakpoints for desktop, tablet and mobile. Production actions are visibly
disabled in the staging shell.
