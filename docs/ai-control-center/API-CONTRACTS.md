# API contracts

Safe aggregation routes are under `/api/v1/ai-control-center`: `navigation`,
`overview`, `health`, `usage`, `integrations`, `feature-flags`,
`security-events`, and `reconciliation`. Each requires a Codestra role and
returns bounded, redacted data.
