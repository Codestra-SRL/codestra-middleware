# Route map

The frontend exposes `/ai/*` with nested operational routes for overview,
jobs, leads, calls, approvals, models, prompts, workflows, integrations,
reconciliation, usage, security, audit and health. Unknown sections render a
clear read-only staging state rather than a blank screen.
