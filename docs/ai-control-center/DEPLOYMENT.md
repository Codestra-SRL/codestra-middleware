# Deployment

Deploy only to isolated staging. Keep `AI_CONTROL_CENTER_PRODUCTION_ACTIONS_ENABLED=false`
and `AI_CONTROL_CENTER_FEATURE_FLAG_WRITES_ENABLED=false`. Runtime deployment
was not attempted because the assigned server rejected SSH authentication.
