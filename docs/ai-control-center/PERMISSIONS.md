# Permissions

Server-provided roles gate Control Center aggregation endpoints. Frontend
navigation is advisory only; backend authorization is mandatory. Tenant and
workspace scope must be applied by middleware before returning any resource.
