# Call access

Call metadata, redacted transcripts and recordings have separate permissions. Recording access uses an expiring protected reference, tenant/call ownership checks and an access audit. Permanent VICIdial URLs and filesystem paths are never returned.
