# Deployment

Deploy only to an approved staging host such as `staging-portal.codestra.co` after DNS, TLS and reverse-proxy access are verified. Production portal, public registration and customer API credentials remain disabled during this mission.
