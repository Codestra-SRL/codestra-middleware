# Broker and exchange adapters

Adapters use secret references, approved authentication, idempotency, rate limits, circuit breakers, signed callbacks, and reconciliation. Only sandbox/demo functions are available.
