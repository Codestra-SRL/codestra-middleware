# API standards

Core endpoints are versioned under `/api/v1/core`. Commands and events require
scope context and an `Idempotency-Key`; malformed or missing context fails closed.
OpenAPI is generated by FastAPI. Authentication, authorization, audit, rate
limits, and persistence are mandatory for enabling mutation routes.
