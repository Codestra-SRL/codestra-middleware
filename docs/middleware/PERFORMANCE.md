# Performance

The target budgets are gateway P95 <100ms, query <150ms, command <250ms,
workflow dispatch <300ms, and event processing <200ms. Runtime measurement is
required before declaring a target passed; this change does not claim production
performance certification.
