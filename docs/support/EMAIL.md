# Email

Email adapters verify inbound authenticity, preserve stable threading, validate attachments, prevent duplicates, handle bounces, and protect reply addresses. Mock delivery is used without credentials.
