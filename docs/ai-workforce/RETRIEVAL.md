# Retrieval

Every retrieval requires tenant, workspace, employee, scope, active-source policy, and bounded result count. Mandatory metadata filters exclude revoked, expired, held, and cross-scope records before vector results are usable.
