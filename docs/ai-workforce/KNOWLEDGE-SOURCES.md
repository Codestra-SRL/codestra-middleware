# Knowledge sources

Sources are tenant/workspace scoped, checksum/versioned, classified, reviewable, expirable, revocable, and indexed only after authorization. Public indexing is never trusted as policy without review.
