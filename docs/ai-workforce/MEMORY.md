# Memory

Memory metadata is authoritative in middleware and must match tenant and workspace exactly. Long-term promotion requires an approved state, source, classification, expiry, and revocation path. Embeddings are private references only.
