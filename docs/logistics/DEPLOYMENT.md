# Staging deployment

Deploy immutable reviewed artifacts with `LOGISTICS_PRODUCTION_ENABLED=false`, notifications off, real routing off, and all automatic-decision flags off. Apply middleware migration before installing the Odoo projection module. No production deployment is authorized.
