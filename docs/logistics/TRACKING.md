# Public tracking

Opaque tokens are stored only as SHA-256 digests, expire, can be revoked, and return minimized milestone data. Invalid, expired, revoked, and enumerated tokens are indistinguishable (`404`).
