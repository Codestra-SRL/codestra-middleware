# Rollback

Disable the staging feature flag, restore the prior frontend artifact, downgrade exactly one migration revision, and uninstall the Odoo projection only after confirming no accounting records reference it. Never restore production during this mission.
