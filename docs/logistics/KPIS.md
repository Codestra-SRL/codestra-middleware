# KPI definitions v1

On-time pickup/delivery compare actual proof timestamps with required windows. Utilization uses assigned capacity hours divided by available hours. Empty distance excludes loaded segments. Gross margin is Odoo-recognized revenue minus approved shipment costs. Claims and damage rates divide affected closed shipments by all closed shipments. All formulas are tenant-scoped and versioned `logistics-kpi-v1`.
