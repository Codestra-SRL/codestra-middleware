# Queue backlog

Inspect middleware and n8n queue depth, retry age, blocked clients, and dead
letters. Do not create uncontrolled retry loops; reconcile durable records
before resuming workers within the approved concurrency limit.
