# Redis unavailable

Confirm the private container, health check, exporter, and dependent-service
logs. Never disable nonce or idempotency validation. Restore the last known
good Compose/ACL backup if a change caused failure, then verify reconnects.
