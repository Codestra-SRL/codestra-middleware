# Recovery identity

Keep the recovery credential outside application containers. Use it only for
ACL recovery, capture a sanitized audit record, rotate it afterward, and keep
the default user disabled.
