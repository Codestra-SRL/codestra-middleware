import {useState} from "react";import {Card} from "../../components/Card";
export function CallbackModule({onSave}:{onSave:(iso:string,reason:string)=>Promise<void>}){const [when,setWhen]=useState("");const [reason,setReason]=useState("");return <Card title="Callback scheduler"><label className="field">Date and time<input type="datetime-local" value={when} onChange={e=>setWhen(e.target.value)}/></label><label className="field">Reason<input value={reason} onChange={e=>setReason(e.target.value)} placeholder="Synthetic test reason"/></label><button disabled={!when||!reason} onClick={()=>void onSave(when,reason)}>Schedule mock callback</button></Card>}
