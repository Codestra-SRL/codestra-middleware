import {useMemo,useState} from "react";import {Card} from "../../components/Card";
export type CallbackPriority="LOW"|"NORMAL"|"HIGH"|"URGENT";
export interface DueCallback{id:string;customer:string;phoneMasked:string;campaign:string;reason:string;scheduledAt:string;customerTimezone:string;lastCall?:string;lastNotes?:string;version:number}
function zonedIso(local:string,zone:string):string{if(!local)throw new Error("Date and time are required");const[d,t]=local.split("T"),[y,m,day]=d.split("-").map(Number),[h,min]=t.split(":").map(Number);let guess=Date.UTC(y,m-1,day,h,min);const f=new Intl.DateTimeFormat("en-CA",{timeZone:zone,year:"numeric",month:"2-digit",day:"2-digit",hour:"2-digit",minute:"2-digit",hourCycle:"h23"});for(let i=0;i<2;i++){const p=Object.fromEntries(f.formatToParts(new Date(guess)).filter(x=>x.type!=="literal").map(x=>[x.type,Number(x.value)]));guess+=Date.UTC(y,m-1,day,h,min)-Date.UTC(p.year,p.month-1,p.day,p.hour,p.minute)}return new Date(guess).toISOString()}
export function CallbackModule({onSave,due}:{onSave:(iso:string,reason:string,timezone:string,priority:CallbackPriority)=>Promise<void>;due?:DueCallback}){const[when,setWhen]=useState(""),[timezone,setTimezone]=useState("America/New_York"),[reason,setReason]=useState(""),[priority,setPriority]=useState<CallbackPriority>("NORMAL"),[status,setStatus]=useState("");const agentTime=useMemo(()=>when?new Intl.DateTimeFormat(undefined,{dateStyle:"medium",timeStyle:"short",timeZone:Intl.DateTimeFormat().resolvedOptions().timeZone}).format(new Date(zonedIso(when,timezone))):"—",[when,timezone]);const save=async()=>{try{await onSave(zonedIso(when,timezone),reason,timezone,priority);setStatus("Callback scheduled")}catch(error){setStatus(error instanceof Error?error.message:"Unable to schedule callback")}};return <Card title="Callbacks">{due&&<section className="callback-popup" role="alertdialog" aria-labelledby="callback-due-title" aria-describedby="callback-due-reason"><header><div><small>CALLBACK DUE</small><h3 id="callback-due-title">{due.customer}</h3></div><time>{new Intl.DateTimeFormat(undefined,{timeStyle:"short"}).format(new Date(due.scheduledAt))}</time></header><p>{due.phoneMasked}</p><dl><div><dt>Campaign</dt><dd>{due.campaign}</dd></div><div><dt>Reason</dt><dd id="callback-due-reason">{due.reason}</dd></div><div><dt>Last call</dt><dd>{due.lastCall||"No prior call"}</dd></div><div><dt>Last notes</dt><dd>{due.lastNotes||"No prior notes"}</dd></div></dl><div className="callback-actions"><button>Call now</button><button className="secondary">Open CRM</button><button className="secondary">Snooze</button><button className="secondary">Reschedule</button></div></section>}<h3>Schedule callback</h3><label className="field">Customer date and time<input type="datetime-local" value={when} onChange={e=>setWhen(e.target.value)} aria-describedby="callback-times"/></label><label className="field">Customer timezone<select value={timezone} onChange={e=>setTimezone(e.target.value)}><option>America/New_York</option><option>America/Santo_Domingo</option><option>America/Chicago</option><option>America/Denver</option><option>America/Los_Angeles</option><option>UTC</option></select></label><div id="callback-times" className="callback-times"><span>Customer <b>{when||"—"} {timezone}</b></span><span>Agent <b>{agentTime}</b></span></div><label className="field">Reason<input value={reason} onChange={e=>setReason(e.target.value)} maxLength={256}/></label><label className="field">Priority<select value={priority} onChange={e=>setPriority(e.target.value as CallbackPriority)}><option>LOW</option><option>NORMAL</option><option>HIGH</option><option>URGENT</option></select></label><button disabled={!when||!reason} onClick={()=>void save()}>Schedule callback</button><span className="sr-status" role="status">{status}</span></Card>}
