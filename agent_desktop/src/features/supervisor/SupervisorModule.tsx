import {Card} from "../../components/Card";import {Badge} from "../../components/Badge";import type {Agent} from "../../types/desktop";
export function SupervisorModule({team}:{team:Agent[]}){return <Card title="Supervisor panel" action={<Badge tone="info">Read-only mock</Badge>}><div className="team">{team.map(a=><div key={a.id}><span className={`presence ${a.status.replace(" ","").toLowerCase()}`}/><span><strong>{a.name}</strong><small>{a.id} · {a.role}{a.activeCall?` · ${a.activeCall}`:""}</small></span><Badge tone={a.status==="Ready"?"good":"neutral"}>{a.status}</Badge><b>{a.quality}%</b></div>)}</div></Card>}
