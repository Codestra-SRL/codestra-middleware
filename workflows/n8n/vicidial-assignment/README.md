# Controlled VICIdial assignment exports

All workflows are inactive, credential-free exports. Middleware owns eligibility, approval, target campaign/list allowlists, idempotency, and the adapter command. No workflow can activate dialing or choose arbitrary identifiers.
