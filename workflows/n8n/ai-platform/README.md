# Sanitized Lead Intelligence workflow exports

All exports are intentionally inactive and contain no credentials, tokens, private endpoints, real lead data, or live HTTP scraping. The controlled synthetic chain is:

`CDA-AI-00` → `CDA-AI-01` → `CDA-AI-02` → `CDA-AI-03` → `CDA-AI-04` → `CDA-AI-05` → `CDA-AI-07`.

`CDA-AI-08` handles bounded failures. `CDA-AI-06` is a hard-fail Odoo import placeholder and must remain disabled.
